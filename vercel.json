{
  "version": 2,
  "buildCommand": "npm run generate",
  "routes": [
      {
          "src": "/api/rebuild",
          "dest": "/server/api/rebuild.post.ts",
          "methods": ["POST"]
      }
  ]
}
